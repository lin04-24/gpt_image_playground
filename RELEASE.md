# V2.4.3（2026-08-29）

### 性能
- 两项启动链路开销削减，缩短 `initStore` 的启动等待：
  - 启动恢复链路并行化：原来「清理孤立图片 → 逐张恢复输入图 → 逐张恢复图库草稿 → 恢复草稿遮罩」全串行执行，每张图都要等上一次 IndexedDB 读取完成。现拆为三条互不依赖的链路（孤立图片清理 / 输入图恢复 / 图库草稿恢复）并发执行，链路内逐张 `await getImageDataUrl` 改为 `Promise.all` 一次发出全部读取。草稿遮罩恢复仍保持在草稿图恢复之后（依赖草稿恢复写回的 `maskDraft`），变更检测逻辑（长度或 dataUrl 变化才写回 store）不变。
  - `cleanupUnreferencedImages` 孤立图片批量删除：原来对每个孤立图片逐条 `await deleteImage`（每次单独开一个事务），现只收集孤立 ID，枚举完成后调用新增的 `db.deleteImages(ids)` 在单个 readwrite 事务里同时删除图片记录和两个缩略图 store 的对应条目。仍保留逐条枚举时的 `isCurrent()` 中止检查与删除前的最终检查，过期清理的防误删语义不变。

### 功能情况
- 并行安全性：清理的引用集合在函数开头同步从 `state.inputImages` 等快照取得，待恢复的图片 ID 全部在引用集合内，恢复链路只读 IndexedDB 不写，不会误删引用图。
- 批量删除 `deleteImages` 与单张 `deleteImage` 的三表联动语义一致（图片 + 大缩略图 + 小缩略图同一事务删除），`deleteImage` 的其余调用点行为不变。
- 未修改持久化结构、数据库版本、云同步接口或 README，升级无需任何迁移操作。

### 验证
- `npm run build` 通过。
- `npm test` 通过（33 个测试文件，254 项测试）。

# V2.4.2（2026-08-29）

### 性能
- 两项搜索路径开销削减，消除任务量大时搜索打字掉帧：
  - 搜索输入加 150ms 防抖：`SearchBar` 的输入框改为本地受控 state，停止输入 150ms 后才写入 store。此前每个按键都会立即触发 `TaskGrid`、`InputBar` 及失败/生成中计数选择器的全表筛选重算。store 中的搜索词被外部改动（如后端同步恢复筛选）时输入框自动同步，组件卸载时清理定时器。
  - `taskMatchesSearchQuery` 按任务引用缓存小写搜索串（模块级 `WeakMap<TaskRecord, string>`）：prompt、`JSON.stringify(task.params)` 与错误信息拼成一段后用一次 `includes` 匹配，替代此前每次匹配都对每个任务重复序列化 params。任务更新时 store 以新对象整体替换，旧对象的缓存随 GC 自然失效，无需手动清理。
- 附带效果：`SearchBar` 的 `failedCount`/`runningCount` 选择器在每次 store 更新时的全表扫描，从"每任务 JSON.stringify"降为"每任务缓存串子串查找"。

### 功能情况
- 搜索语义不变：prompt、参数、错误信息三个字段的匹配范围与原来完全一致（分段拼接用 `\n` 分隔，而查询词来自单行输入且经过 trim，不可能包含 `\n`，因此拼接后 includes 与分段匹配等价）。
- 交互行为不变：输入框回显仍即时，仅筛选结果延迟 150ms 生效；清除失败/生成中任务等基于搜索词的统计与确认流程不受影响。
- 未修改持久化结构、数据库版本、云同步接口或 README，升级无需任何迁移操作。

### 验证
- `npm run build` 通过。
- `npm test` 通过（33 个测试文件，254 项测试）。

# V2.4.1（2026-08-29）

### 性能
- 两项持久化开销削减，消除画遮罩后打字卡顿：
  - `persist` 的 localStorage 写入加防抖：新增 `src/lib/persistStorage.ts`，连续 `set()` 在 500ms 内合并为一次写入，页面隐藏（`visibilitychange hidden`）或关闭（`pagehide`）时冲刷未落盘数据，页签关闭不丢数据。此前 zustand persist 默认每次 `set()` 都同步 `JSON.stringify` 并整段写入 localStorage。
  - 遮罩草稿不再把全分辨率 PNG base64 写进 localStorage：画完遮罩后异步 `storeImage` 入 IndexedDB 并回填 `maskImageId`，持久化草稿时 `maskDataUrl` 剥离为空串、只保留 `maskImageId`。入库前（毫秒级窗口）暂留原图，避免刚画完遮罩就关页签导致遮罩丢失。此前画了遮罩之后每敲一个字符都会同步序列化并写入多 MB 文本，明显卡顿且逼近 localStorage 5-10MB 配额。

### 功能情况
- 交互行为与数据语义不变：遮罩的绘制、预览、提交、复用任务遮罩等流程与原实现一致；遮罩图片按内容哈希去重，提交路径行为不变。
- 恢复路径补齐：刷新后草稿遮罩按 `maskImageId` 从 IndexedDB/远端取回原图；旧版本持久化的完整 `maskDataUrl` 草稿仍正常恢复，随下一次写入自动收敛为新格式。
- `cleanupUnreferencedImages` 把草稿遮罩 ID 计入引用，未随任务提交的遮罩记录不会被启动清理误删。
- 未修改数据库结构（DB_VERSION 仍为 5）、云同步接口或 README，升级无需任何迁移操作。

### 验证
- `npm run build` 通过。
- `npm test` 通过（33 个测试文件，254 项测试）。

# V2.3.4（2026-08-29）

### 性能
- 两项全局开销削减：
  - `TaskCard` 的 store 订阅收窄：原来每张卡订阅整个 `s.settings`，任何设置变化都会导致全列表卡片重渲染；现改为只订阅实际用到的 `s.settings.alwaysShowRetryButton`（布尔值，文件内唯一消费点），其余设置变更不再波及任务网格。
  - `db.ts` 复用 IndexedDB 连接：模块级缓存 `openDB()` 的连接 promise，所有任务/图片/缩略图读写不再每次操作都执行一次 `indexedDB.open`。失败或连接意外关闭（`onclose`）时重置缓存，后续操作可重新打开；注册 `onversionchange`，其他标签页升级数据库版本时主动让出连接，避免阻塞升级。

### 功能情况
- 交互行为与数据语义完全不变：卡片渲染结果一致，IndexedDB 的库名、版本（DB_VERSION 仍为 5）、store 结构与读写顺序均未改动，升级无需任何迁移操作。
- 未修改云同步接口或 README。

### 验证
- `npm run build` 通过。
- `npm test` 通过（32 个测试文件，248 项测试）。

# V2.3.3（2026-08-29）

### 性能
- 任务网格三项渲染优化，显著降低长列表与框选时的卡顿：
  - `TaskCard` 增加 `React.memo`，回调改为稳定引用（由卡片调用时携带 task 派发，`onReuse`/`onEditOutputs` 直接复用模块级 action，点击与删除回调 `useCallback` 固化）。此前框选每次 `mousemove` 触发 `setSelectedTaskIds` 会导致全部卡片重渲染，现在只有选中状态真正变化的卡片才会重渲染。
  - 框选命中计算（`querySelectorAll` + 逐卡 `getBoundingClientRect` + `setState`）改为 `requestAnimationFrame` 每帧合并执行一次；`mousemove`/`scroll` 高频事件里只记录最新坐标。松开鼠标时同步执行挂起的最后一帧，快速拖放不会丢失末次命中；框未扫过新卡片时跳过 store 写入，避免无效的全列表渲染。
  - 选中集合改为 `useMemo` 派生的 `Set` 做成员判断，替代原来对每张卡片执行 `Array.includes` 的 O(n²) 开销。
- `.task-card-wrapper` 增加 `content-visibility: auto` + `contain-intrinsic-size`，浏览器跳过视口外卡片的渲染与布局，长列表滚动更流畅（已知取舍：远离视口的卡片在框选时返回估算矩形，滚动到可视区后自动校正）。

### 功能情况
- 交互行为不变：框选、Ctrl/⌘ 点选、触屏侧滑、点击打开详情、按钮操作等均与原实现一致。
- 未修改数据库结构（DB_VERSION 仍为 5）、云同步接口或 README，升级无需任何迁移操作。

### 验证
- `npm run build` 通过。
- `npm test` 通过（32 个测试文件，248 项测试）。

# V2.3.2（2026-08-29）

### 性能
- 缩略图空闲回填选批不再加载整张原图：`getNextThumbnailBackfillBatch` 此前为决定并发度对每个候选调用 `getImage(id)`，把多 MB 的完整原图 dataUrl 读进内存只为拿 width/height。现在改为读取 720 大档缩略图记录——该记录的 `width/height` 本身就是原图尺寸——读取体积从 MB 级降到几十 KB；仅当缩略图记录缺失或无尺寸时才回退读原图（基本只覆盖缩略图创建失败的极端情况），尺寸完全未知时维持「并发 1」的保守策略，回填行为语义不变。

### 功能情况
- 未修改数据库结构（DB_VERSION 仍为 5）、缩略图双档协议、云同步接口或 README，升级无需任何迁移操作。
- 回填并发度决策结果与原实现一致：缩略图记录与原图记录的宽高写入路径相同（`createImageThumbnail` 返回原图尺寸并同步写入两档）。

### 验证
- `npm run build` 通过。
- `npm test` 通过（32 个测试文件，248 项测试）。

# V2.3.1（2026-08-29）

### 修复
- 后端模式 `thumbnail.ready` SSE 事件不再清空全部图片缓存：此前服务端任一缩略图就绪都会调用 `clearImageCaches()`，把原图 LRU（8 条）与缩略图 LRU（80 条）整体清空，所有可见卡片被迫重新回源、详情大图也要重新拉取。现在从事件 payload 解析 `imageId`，只调用 `deleteImageCacheEntry(id)` 失效对应图片，并立即重新拉取该图缩略图——成功后经 `storeAndPublishImageThumbnail` 派生 320 小档并通知订阅方，对应卡片自动更新，其余卡片完全不受影响。
- 事件解析失败或缺少 `imageId` 时保留原有全量清空行为作为兜底（SSE payload 属外部输入），正确性不回退。

### 功能情况
- 仅影响后端模式的事件处理路径；纯浏览器模式与云端直连模式不消费 `thumbnail.ready` 事件，行为不变。
- 未修改数据库结构（DB_VERSION 仍为 5）、公开 API 返回格式或 README，升级无需任何迁移操作。

### 验证
- `npm run build` 通过。
- `npm test` 通过（32 个测试文件，248 项测试）。

# V2.3.0（2026-08-29）

### 性能
- 缩略图拆为双档，网格降为 320px 小档：此前缩略图统一为 720px/webp q0.9，而网格卡片显示宽度 160px（DPR=2 也只需 320px），缩略图体积约为网格实际所需的 4-8 倍。现在网格卡片与收藏夹封面读取 320px/q0.75 小档，IndexedDB 读取体积、内存占用与首屏解码量同步下降；详情弹窗占位图保留 720 大档，大档缺失时退回小档，详情预览质量不变。
- 存量数据回填不重新解码原图：升级后首次展示某张图时，从已有 720 大档现场降采样生成 320 小档并落库（`deriveSmallImageThumbnail`），每张图只做一次，无需全量扫描、不触碰 4K 原图。

### 功能情况
- 新图入库时一次解码同时产出两档：`createImageThumbnail` 先绘 720 canvas、再二次缩放出 320 canvas，编码两个 webp 后同写两个 store；storeImage、旧图缩略图重生成、云端缩略图下载、空闲回填等所有写库路径行为一致。
- 小档存入新 object store `thumbnails_small`，`DB_VERSION` 4→5，IndexedDB 打开时自动建库，升级无需手动迁移；大档 `thumbnailVersion` 保持 2 不变，已有大档记录全部继续有效，云端/后端同步协议（服务端仅存一份 720 缩略图）不受影响，后端模式下载大档后在本地派生小档。
- 网格内存缓存（`thumbnailCache`）与订阅推送（TaskCard、收藏夹卡片）只消费 320 小档；详情弹窗改用 `ensureLargeImageThumbnailCached` 入口优先取 720 大档。
- ZIP 导出/导入与云端上传仍使用 720 大档；旧备份导入后小档按需惰性派生。任务卡片宽高比显示不变：小档记录携带的 width/height 仍为原图尺寸。
- 流式中间图 `skipThumbnail` 路径依旧不产出任何档；`deleteImage`/`clearImages` 同步清理小档，删除恢复路径同步恢复。

### 验证
- `npm run build` 通过。
- `npm test` 通过（32 个测试文件，248 项测试）。

# V2.2.3（2026-08-29）

### 性能
- 流式中间预览图落库跳过缩略图生成：`persistTaskStreamPartialImage` 对每张 partial 图原先走完整 `storeImage` 流程（SHA-256 哈希、缩略图大图解码 + canvas 绘制 + webp 编码、IndexedDB 双写），`partial_images=3` × 批量 n 张时生成过程中主线程反复做大图编解码，而这些图任务完成后即被删除。现在 `storeImage`/`storeImageWithSize` 新增 `skipThumbnail` 选项，中间图落库只写原图记录、不生成缩略图、不写宽高，生成期间的落库工作量约减半。
- 缩略图回填队列排除流式中间图：仅作为 `streamPartialImageIds` 引用的图片没有缩略图也不再进入 `scheduleThumbnailBackfill` 队列。此前启动时和每次云同步后 `cleanupUnreferencedImages` 会把所有被引用图片排进回填，跳过的缩略图会在空闲回填里照样生成一遍（后端模式云同步频繁，不排除会抵消上述收益）。

### 功能情况
- 中间图全链路不经缩略图：生成中预览走内存原图 dataUrl，事后仅详情弹窗「下载中间步骤图」提供原图下载，跳过缩略图不影响任何展示路径；hash 去重、`requestIndex` 语义、任务完成后中间图清理行为均保持不变。
- 同一图片先作为中间图、后又作为正式输出（hash 相同）时，输出存储仍走既有修补路径补齐缩略图与宽高；云端上传对缺失宽高本就按条件携带 header，中间图无宽高不影响云同步。
- 未修改数据库结构（DB_VERSION 仍为 4）、公开 API 返回格式或 README，升级不需要额外迁移操作。

### 验证
- `npm run build` 通过。
- `npm test` 通过（32 个测试文件，248 项测试）。

# V2.2.2（2026-08-29）

### 性能
- 编辑请求输入图/遮罩 Blob 只转换一次：images 模式并发 n 路与自定义服务商 multipart 模板此前会在每一路独立执行 canvas 解码+重编码，同一张 4K 输入图被重复转换 n 次，浪费且阻塞主线程。现在在 `callOpenAICompatibleImageApi` 入口统一预转换一次，通过 `inputImageBlobs`/`maskBlob` 传入并发各路复用（Blob 跨 FormData 复用是安全的）；responses 模式与自定义服务商 JSON 提交不涉及 Blob 转换，不做无谓预转换。
- 输出图并行落库：`storeTaskOutputImages` 由逐张串行改为 `Promise.all` 并行，整串 base64 的 SHA-256 哈希、缩略图生成与 IndexedDB 写入互相重叠，n 张 4K 输出图的任务完成耗时从串行累加降为最慢单张耗时；任一张失败仍统一清理已写入的图片并整体报错，错误语义与清理行为保持不变。
- 缩略图异步编码：`createImageThumbnail` 的 `canvas.toDataURL('image/webp')` 换成异步 `canvas.toBlob` 再转 data URL，浏览器可将编码移出主线程，减少大图缩略图生成对主线程的阻塞；缩略图尺寸、质量与版本号均未变化。
- 透明背景后处理移出关键路径：透明背景任务不再等待逐张 CPU 泛洪填充 + 全尺寸 PNG 重编码，先以原图作为输出立即标记任务完成，浏览器空闲时（requestIdleCallback，最长 2 秒兜底）再逐张透明化并替换输出；替换前校验任务该位置的输出未被变更，任务被删除或结果已替换时丢弃处理结果，不产生孤儿图片。处理失败仍以原图展示，`transparentOriginalImages` 对应位置按原语义标记为失败。

### 功能情况
- 编辑请求的请求体结构、并发上限（6）、429/408/5xx 自动重试、failedRequests 上报、流式部分图回调的 requestIndex 语义均保持不变；每路请求仍独立构建 FormData 与超时控制。
- 透明背景任务完成瞬间会先短暂显示原图，空闲处理完成后数秒内自动替换为透明版本；若图内无键色背景（透明化后字节级相同），会一直显示原图，与旧版按内容去重时的表现一致。
- 未修改数据库结构、公开 API 返回格式或 README，升级不需要额外迁移操作。

### 验证
- 新增 `src/lib/openaiCompatibleImageApi.test.ts` 4 项测试：images 并发编辑 n=3 时输入图 Blob 只转换 1 次；带遮罩时第一张转 PNG、其余转原始 Blob、遮罩各自只转换 1 次；自定义服务商 multipart 编辑只转换 1 次；自定义服务商 JSON 提交不做预转换。
- `npm run build` 通过。
- `npm test` 通过（32 个测试文件，248 项测试）。

# V2.2.1（2026-08-29）

### 功能
- 批量生图请求改为有界并发池：同时最多 6 个请求在飞，完成一个补一个。此前按数量 n 一次性发出全部请求，HTTP/1.1 下浏览器对同域约只有 6 个连接，超出的请求只会排队，拖慢首批完成时间，也更容易触发服务商 429 限流；images 与 responses 两条并发路径均已接入。
- 并发槽位失败自动重试：单请求遇到 408/429/5xx 时自动重试一次，重试前按 1 秒基础指数退避加随机抖动（实际约 0.5–1.5 秒）等待，避免多个槽位同时失败后集中在同一时刻重试；重试成功后不再计入 failedRequests，减少批量任务因单点限流整体失败、需要手动重跑整个任务的情况。
- 重试等待期间该槽位继续占用并发额度，对正在限流的服务商保持背压，重试不会与排队请求同时压上去。

### 功能情况
- 默认并发上限为 6，与浏览器同域连接数对齐；流式部分图回调的 requestIndex 语义不变，失败槽位仍按原槽位序号报告。
- 重试只发生在请求尚未返回任何部分图就被服务端拒绝的场景：请求超时 abort、流中途解析错误没有 HTTP 状态码，不会触发自动重试。
- 未修改数据库结构、公开 API 返回格式或 README，升级不需要额外迁移操作。

### 验证
- 新增 `src/lib/openaiCompatibleImageApi.test.ts`（4 项测试）：n=10 时同时在飞请求不超过 6 且全部成功；images 路径首个请求 429 后自动重试成功且无 failedRequests；400 不重试并正常记入 failedRequests；responses 路径 503 同样自动重试成功。
- `npm run build` 通过。
- `npm test` 通过（32 个测试文件，244 项测试）。

# V2.2.0（2026-08-29）

### 修复
- 修复后端模式下刷新页面、F5 或重新打开浏览器后，默认「全部」筛选明明已成功请求到任务数据却仍显示空列表，必须手动切换到「已完成」才出现任务卡的问题。原因是任务网格的缓存计算遗漏后端分页初始化状态依赖，首次同步时会永久复用初始化阶段的空数组；现在分页状态完成初始化后会立即重新计算并渲染任务卡。
- 移除「全部」任务列表对 Redis 任务 ID 索引的运行时依赖，直接以 PostgreSQL 查询结果作为权威数据，避免 Redis 索引与数据库 revision 不一致时返回永久空列表。
- Redis 不再缓存空任务页，并隔离旧版本可能写入的空列表缓存；容器启动或数据迁移窗口产生的短暂空结果不会继续影响后续刷新。
- 浏览器数据迁移和旧 SQLite 数据迁移写入任务后会递增任务列表 revision，确保现有分页缓存及时失效。

### 功能情况
- 默认「全部」、已完成、生成中、失败筛选均继续支持，刷新或重新打开浏览器后无需手动切换筛选即可加载任务。
- 服务端分页、提示词搜索、收藏夹筛选、任务创建与 SSE 状态刷新保持原有行为。
- 未修改数据库结构、公开 API 返回格式或 README，升级过程不需要额外数据迁移操作。

### 验证
- 使用真实 Chromium 浏览器复现旧行为：`GET /api/tasks?page=1` 返回 2 个任务，但首次「全部」页面任务卡数量为 0；切换到「已完成」后显示 2 个任务。
- 修复后使用当前源码启动 Vite，并在浏览器中模拟后端首次返回任务：默认「全部」页面首次加载直接显示任务卡，无需切换筛选；已保存验证截图。
- `npm run build` 通过。
- `npm test` 通过（31 个测试文件，240 项测试）。

# V2.1.3（2026-08-29）

### 修复
- 修复重开浏览器后所有修改操作提示「CSRF Token 无效」的问题（新开标签页同样受影响）。原因：会话 cookie 为持久 cookie（30 天有效），关闭浏览器不失效，但 CSRF Token 存储在前端 sessionStorage 中，关闭浏览器即被清空；重开浏览器后会话检查仅返回「已登录」，前端取不回 Token，所有写请求因缺少 `X-CSRF-Token` 头被服务端拒绝，只能重新登录恢复。现在 `/api/auth/session` 与 `/cloud-api/session` 在已登录时返回 csrfToken，前端会话检查时自动写回 sessionStorage，重开浏览器、新开标签页或清除站点存储后均无需重新登录。

### 验证
- `npm run build` 通过。
- `npm test -- --run` 通过（31 个测试文件，240 项测试）。

# V2.1.2（2026-08-29）

### 修复
- 修复后端模式在 `docker compose up -d --build --force-recreate` 重建更新后，刷新页面默认「全部」筛选下任务列表一直为空、必须手动切换到其他筛选状态才加载任务的问题。原因：容器重启窗口内首次任务同步失败后前端不会自动重试，SSE 重连时又因浏览器没有 Last-Event-ID 而收不到全量同步事件，页面无从自愈。现在同步失败后按 2s → 4s → 8s → 15s → 30s（封顶）的间隔自动重试，服务恢复后任务列表自动加载，无需任何手动操作；同步成功或退出后端同步时重置重试状态。
- 修复 API 服务在 Redis 重启时直接崩溃的问题：SSE 事件订阅用的 Redis 复制连接缺少 error 事件监听，Redis 断连会以未处理异常击溃进程（实测复现），靠 `restart: unless-stopped` 才能拉起并拉长服务中断窗口。现在断连仅记录错误日志，客户端自动重连，进程保持存活。

### 验证
- 本地复现环境（独立 PostgreSQL/Redis + API + 浏览器）端到端验证：重建重启窗口内制造同步失败后，服务恢复约 20 秒内错误提示自动消失、任务列表自动恢复；Redis 重启后 API 进程存活并继续响应请求。
- `npm run build` 通过。
- `npm test -- --run` 通过（31 个测试文件，240 项测试）。

# V2.1.1（2026-08-29）

### 修复
- 提交生图任务即时反馈：本地模式不再阻塞等待输入图重复持久化，点击后任务卡立即出现；后端模式在上传图片、创建任务期间先插入本地"排队中"占位卡片，服务端任务返回后原位替换，提交失败自动移除占位。
- 排队（queued）状态的任务卡现在同样显示生成中转圈，不再出现"卡片已出现但过几秒才转圈"的空窗。
- 任务计时钳制为非负，修复后端模式下服务端时钟略超前本地时，任务卡左上角计时从 -1:-1 起跳的问题。
- 修复整页缩略图闪烁：后端同步每次 SSE 事件会用新对象整体替换任务列表，此前缩略图加载按数组引用触发并先清空再加载，导致整页缩略图集体白一帧；现改为按图片 id 触发且保留已有缩略图。
- 后端同步对内容未变化的任务保留原对象引用，避免每次 SSE 事件整列表重渲染带来的卡顿。

### 验证
- `npm run build` 通过。
- `npm test -- --run` 通过（31 个测试文件，240 项测试）。

# V2.1.0（2026-08-29）

### 功能
- 生图配置中的「审核」选项已移除，原位置改为「批次」输入框（默认 1）。
- 点击一次生图按钮可按批次 N 连续发起 N 次独立的生图请求，与「数量」（单次请求内的出图数 n）互相独立：批次控制请求次数，数量控制每次请求的出图张数。
- 相邻两次生图请求之间加入毫秒级（个位数，5ms）延迟，N 次请求依次错开发出，互不等待完成。
- 批次值提交时自动钳制为 1–999 的整数；批次大于 1 时提交成功提示「已提交 N 个生成任务」。审核参数底层管道保留默认值 auto，历史任务详情展示与自定义服务商模板不受影响。

### 验证
- `npm run build` 通过。
- `npm test -- --run` 通过（31 个测试文件，240 项测试）。

# V2.0.3（2026-08-28）

### 功能
- 设置 → API 配置新增「尺寸参数格式」选项：可选择宽高比（默认，如 1:1、2:3、16:9）或像素尺寸（M×N，如 1024x1536）两种 size 参数格式。
- 发送请求时自动把不符合所选格式的参数转换成符合的参数：宽高比模式将像素尺寸转为简化比例（1024x1536 → 2:3），像素尺寸模式将比例转为 1K 档位像素（2:3 → 1024x1536）。
- 转换在请求入口统一生效，覆盖 OpenAI 兼容 images、responses 与自定义 HTTP 服务商模板（`$params.size`）三类请求路径；Codex CLI 兼容模式注入的分辨率提示语随所选格式变化，fal.ai 请求不受影响。
- 该设置按 API 配置（Profile）独立保存，随配置导入导出和切换服务商保留；仅影响发出的请求，应用内仍按像素尺寸显示与选择。

### 验证
- `npm run build` 通过。
- `npm test -- --run` 通过（31 个测试文件，240 项测试）。

# V2.0.2（2026-08-28）

### 修复
- 后端分页同步期间不再清空任务列表，保留当前页数据，新页到达后整体替换，消除筛选切换和翻页时的闪烁。
- 新增首次同步完成标记：首次同步完成前不渲染本地缓存任务，避免启动时闪现 IndexedDB 中的全量旧照片。
- 首次同步失败时只显示错误信息，不再把 IndexedDB 旧历史误当作当前列表展示，解决列表"时有时无"的现象。
- 提交任务的乐观更新保留至服务端列表确认替换，新任务不再短暂显示后消失。

### 功能保持
- 服务端分页每页最多 30 条，页码、上一页、下一页、提示词搜索、状态筛选和收藏夹筛选继续覆盖完整历史数据。

### 验证
- `npm run build` 通过。
- `npm test -- --run` 通过（31 个测试文件，236 项测试）。

# V2.0.1（2026-08-28）

### 修复
- 修复后端分页模式下前端仍从 IndexedDB 全量缓存展示历史任务的问题，分页请求期间只显示当前服务端页。
- 任务列表和乐观更新统一限制为每页最多 30 条，避免新建或重试任务造成页内超量展示。
- 兼容旧 SQLite 快照分页接口的默认和最大页大小统一为 30 条，并补充超限参数测试。

### 功能保持
- PostgreSQL 任务列表继续支持覆盖完整历史数据的提示词搜索、状态筛选和收藏夹筛选。
- 保留页码、上一页和下一页导航，以及每个任务的全部输出图片。

### 验证
- `npm run build` 通过。
- `npm test -- --run` 通过（31 个测试文件，236 项测试）。
- `git diff --check` 通过。

## V2.0.0（2026-08-28）

### 后端架构
- 完成 Redis + PostgreSQL 后端化改造：Fastify API、独立 Worker、PostgreSQL 和 Redis 由 Docker Compose 统一运行。
- PostgreSQL 成为任务、图片元数据、Profile、收藏夹和应用状态的权威数据源，IndexedDB 降级为浏览器缓存。
- Redis 提供任务队列、可重建的分页索引、热点缓存、会话和 SSE 事件；支持租约恢复、重试和 Outbox 投递。
- 所有生图请求移至服务端 Worker，支持 OpenAI 兼容接口、fal.ai 和自定义 HTTP 服务商。

### 数据与安全
- 服务端目录保存原图和缩略图，采用分片路径、原子写入、SHA-256 校验和受保护读取。
- API Key 使用 AES-256-GCM 加密存储，仅在 Worker 内存中解密，接口和日志不返回敏感字段。
- 新增登录会话、CSRF、Origin 校验、请求参数校验和 Redis 登录限流。
- 新增 SQLite 与浏览器 IndexedDB 的 dry-run、apply、verify 迁移流程，以及 Redis 重建命令。

### 前端体验
- 任务列表改为服务端分页，每页最多 30 条，搜索、状态和收藏筛选覆盖完整历史。
- 通过 SSE 接收任务状态、缩略图就绪和同步提示，断线后自动重新查询权威数据。

### 部署与验证
- 新增 `deploy/cloud/docker-compose.yml`、`deploy/cloud/.env.example` 和 API/Worker 健康检查。
- `npm run build` 通过；`npm test` 通过（31 个测试文件，235 项测试）。

## v1.0.0（2026-08-26）

### 修复
- 修复 `grok-imagine-image-2.0` 生成图片时的尺寸参数兼容问题。
- 将应用内尺寸自动换算为上游原生 `aspect_ratio`：例如 `2560x1440` 对应 `16:9`，避免模型回退为默认 `2:3` 图片比例。
- 覆盖 Images API、图像编辑、Responses API 与批量生成，保持各入口的比例行为一致。

### 部署
- Docker 更新命令明确为 `docker compose up -d --build --force-recreate`，避免仅重建容器而继续复用旧镜像。

### 验证
- Vitest：408 项测试通过。
- 生产构建：TypeScript 与 Vite 构建通过。

## v0.2.0（2026-08-19）

### 性能优化
- 生成图片完成后自动生成并持久化小尺寸缩略图。
- 首页任务卡片和收藏夹封面优先使用缩略图，减少大量历史图片同时打开时的内存占用和解码压力。
- 详情弹窗改为按当前图片按需加载原图，切换多图结果时才继续加载下一张。
- 原图加载完成前显示缩略图，降低打开详情时的等待感。

### 兼容性
- 继续保留 IndexedDB 中原图存储、全屏预览、下载、编辑和导出能力。
- 对已有图片支持后台补生成缩略图，不需要重新生成历史任务。

## v0.7.3（2026-08-01）

### 修复
- 修复 Service Worker 缓存同源动态 GET 请求的问题：仅缓存应用外壳及构建静态资源，避免异步生图轮询接口持续返回首次缓存的处理中状态，并在更新后自动清理旧缓存 (#127)。
